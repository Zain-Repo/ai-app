# Vercel provider catalog

## Outcome

Added Vercel to the provider connection dialog's direct-provider catalog so it
appears in the provider list and can be found through provider search.

## Validation

- Provider dialog tests passed.
- Prettier checks passed for the changed TypeScript files.

## Scope

This change adds the provider entry to the catalog. It does not add a Vercel
credential flow or model integration.